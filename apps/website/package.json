{
  "name": "@aerograph/website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "bun run build && wrangler dev",
    "deploy": "bun run build && wrangler deploy",
    "test": "playwright test",
    "social": "node scripts/social.mjs",
    "lint": "biome check public scripts tests playwright.config.mjs package.json wrangler.jsonc",
    "lint:fix": "biome check --write public scripts tests playwright.config.mjs package.json wrangler.jsonc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "wrangler": "^4.0.0"
  }
}
